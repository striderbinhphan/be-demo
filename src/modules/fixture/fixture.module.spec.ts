import { FixtureModule } from './fixture.module';

describe('FixtureModule', () => {
  it('should create an instance of FixtureModule', () => {
    const fixtureModule = new FixtureModule();
    expect(fixtureModule).toBeInstanceOf(FixtureModule);
  });
});
